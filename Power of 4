class Solution {
    public boolean isPowerOfFour(int n) {
        for(int i=0;i<=15;i++){
            int p=(int) Math.pow(4,i);
            if(p==n) return true;
        }
        return false;
    }
}
